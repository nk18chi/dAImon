<!-- Generated by `make docs` from daemon.toml + skill/SKILL.md. Do not edit by hand. -->

# mongo-slow-query-reviewer

Review MongoDB Atlas query shapes, find the ones costing the most execution time, and file one Shortcut story per new offender with a proposed index or rewrite.

- **Command:** `/mongo-slow-query-reviewer`
- **Schedule:** 09:00,16:00 mon-fri
- **Source:** mongodb, shortcut (read-write)
- **Required inputs:** `atlas_project_id`, `cluster_name`
- **Backend:** `claude`

## Inputs

| Input | Default | Notes |
|-------|---------|-------|
| `bot_marker` | `🤖` | Prefix on every Shortcut story this daemon creates, so its stories are identifiable and reply/triage daemons can tell them apart. |
| `max_new_stories` | `3` | Most new query shapes to file as stories in a single run, highest total execution time first. Caps the blast radius of a bad deploy that introduces several slow shapes at once; the rest wait for the next run. |
| `ignore_namespaces` | — | Namespaces to ignore entirely, comma-separated (substring match on "db.collection"). Use for collections whose slowness is known and accepted — batch jobs, migrations, analytics scratch space. |
| `story_labels` | `perf-database` | Labels stamped on every story this daemon files, comma-separated. These are categorization labels, NOT the ai-ready/ai-assist assessment labels — the daemon must still leave assessment to story-reviewer. |
| `story_type` | `chore` | Tracker work-item type for filed stories. Performance work is maintenance, not new capability, so it lands as a chore. |
| `epic_id` | `47834` | Epic filed stories group under. Pinned rather than left to the agent to infer — an inferred epic can drift between runs, scattering the backlog. |

## Gate

`discover.sh` decides whether the daemon fires on a given run.

See [skill/SKILL.md](skill/SKILL.md) for the full prompt.
